[project]
name = "annamalaiyar-trust"
version = "0.1.0"
description = "Annamalaiyar Trust member portal - registration, agent verification, vouchers, catalogue, expo"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "flask-cors>=6.0.2",
    "flask-sqlalchemy>=3.1.1",
    "python-dotenv>=1.2.2",
    "gunicorn>=23.0.0",
]
